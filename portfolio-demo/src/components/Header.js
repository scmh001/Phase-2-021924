import React from 'react' //allows us to use React library

function Header() { //declares component (each component is a function)
    //return what we want to see on page 
  return (
    <div>
        <h1>Header</h1>
    </div> 
  )
}

//export the functional component so that other components can use it
export default Header
