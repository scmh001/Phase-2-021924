import React from 'react' //import code from react library (to enable react)

function Form() { //define functional component
  return ( //return JSX to be rendered onto the page (as HTML)
    <h2>Form</h2>
  )
}

export default Form //export functional component for parent components to use 
